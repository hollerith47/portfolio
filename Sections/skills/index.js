import SkillsContainer from "./SkillsContainer";

const Skills = ({skillsRef}) => {
    return (
        <>
            <SkillsContainer
                skillsRef={skillsRef}
            />
        </>
    );
};

export default Skills;